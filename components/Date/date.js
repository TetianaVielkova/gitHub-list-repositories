// import { parseISO, format } from 'date-fns';

// export default function Date({ dateString }) {

//   const date = parseISO(dateString);
//   return <time style={{ fontSize: '16px' }} dateTime={dateString}>{format(date, 'd-MM-yyyy')}</time>;
// }

